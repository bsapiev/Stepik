#include <iostream> 
using namespace std; 
int main(){ 
    int a, b, c, d, e, f;  
    cin >> a >> b >> c; 
    d = (a + 2 - 1) / 2; 
    e = (b + 2 - 1) / 2; 
    f = (c + 2 - 1) / 2; 
    cout << d + e + f;
    return 0;
} 
